# تبدیل واحد - Telegram Mini App

یک مینی اپلیکیشن تلگرام برای تبدیل سریع و آسان واحدهای مختلف (طول، وزن، دما، حجم، زمان، مساحت، سرعت) با رابط کاربری حرفه‌ای و انیمیشن‌های زیبا.

## ویژگی‌ها

- ✅ تبدیل 7 دسته مختلف واحد (طول، وزن، دما، حجم، زمان، مساحت، سرعت)
- ✅ رابط کاربری فارسی و RTL
- ✅ انیمیشن‌های حرفه‌ای با Framer Motion
- ✅ طراحی Responsive برای موبایل
- ✅ پشتیبانی از Dark Mode (اتوماتیک از Telegram)
- ✅ بدون نیاز به API، دیتابیس یا ثبت نام
- ✅ امنیت بالا با Sanitization ورودی‌ها
- ✅ بهینه‌سازی Performance و Bundle Size
- ✅ صفحه Welcome با دایره شیشه‌ای نمایش تبدیل‌های زنده
- ✅ طراحی مدرن با فونت‌های بزرگ و بولد برای خوانایی بهتر

## تکنولوژی‌های استفاده شده

- **React 19** - کتابخانه UI
- **Vite** - Build tool و bundler
- **Tailwind CSS** - استایل‌دهی
- **Framer Motion** - انیمیشن‌ها
- **Telegram Web App SDK** - اتصال به تلگرام

## ساختار پروژه

```
convert/
├── src/
│   ├── components/          # کامپوننت‌های UI
│   │   ├── CategorySelector.jsx
│   │   ├── UnitSelector.jsx
│   │   ├── NumberInput.jsx
│   │   └── ResultDisplay.jsx
│   ├── constants/          # داده‌های ثابت
│   │   └── units.js        # واحدها با نام‌های فارسی
│   ├── hooks/              # Custom Hooks
│   │   └── useTelegram.js  # Hook برای Telegram API
│   ├── types/              # Type Definitions
│   │   └── conversion.js
│   ├── utils/              # توابع کمکی
│   │   ├── conversions/    # منطق تبدیل واحدها
│   │   │   ├── length.js
│   │   │   ├── weight.js
│   │   │   ├── temperature.js
│   │   │   ├── volume.js
│   │   │   ├── time.js
│   │   │   ├── area.js
│   │   │   ├── speed.js
│   │   │   └── index.js
│   │   ├── telegram.js      # توابع کمکی Telegram
│   │   └── validation.js   # اعتبارسنجی و Sanitization
│   ├── App.jsx             # کامپوننت اصلی
│   ├── main.jsx            # Entry point
│   └── index.css           # استایل‌های global
├── index.html
├── vite.config.js          # تنظیمات Vite
├── tailwind.config.js      # تنظیمات Tailwind
├── vercel.json             # تنظیمات Vercel
└── package.json
```

## نصب و راه‌اندازی

### پیش‌نیازها

- Node.js 18+ و npm

### مراحل نصب

1. کلون کردن یا دانلود پروژه:
```bash
cd convert
```

2. نصب وابستگی‌ها:
```bash
npm install
```

3. اجرای پروژه در حالت Development:
```bash
npm run dev
```

4. ساخت نسخه Production:
```bash
npm run build
```

5. پیش‌نمایش نسخه Production:
```bash
npm run preview
```

## Deploy در Vercel

### پیش‌نیازها

```bash
# نصب Vercel CLI (اگر نصب نشده)
npm i -g vercel
```

### روش 1: Deploy از GitHub (توصیه می‌شود)

1. پروژه را در GitHub push کنید:
```bash
git add .
git commit -m "Initial commit"
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

2. به [Vercel](https://vercel.com) بروید و وارد شوید
3. روی "New Project" کلیک کنید
4. Repository را انتخاب کنید
5. تنظیمات:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. روی "Deploy" کلیک کنید
7. بعد از deploy موفق، URL را کپی کنید (مثلاً: `https://your-app.vercel.app`)

### روش 2: Deploy با Vercel CLI

```bash
# Login به Vercel (اولین بار)
vercel login

# Deploy به Preview
npm run deploy:preview

# Deploy به Production
npm run deploy
```

### تنظیمات Telegram Bot

> ⚠️ **مهم**: این پروژه **نیازی به Bot Token ندارد**! فقط یک ربات ساده بسازید و Mini App را متصل کنید.

بعد از Deploy:

1. به [@BotFather](https://t.me/botfather) در تلگرام بروید
2. دستور `/newbot` را ارسال کنید و یک ربات بسازید
   - BotFather یک Token به شما می‌دهد، اما **نیازی به آن ندارید**!
3. دستور `/newapp` را ارسال کنید
4. ربات خود را انتخاب کنید
5. اطلاعات را وارد کنید:
   - **Title**: `تبدیل واحد`
   - **Description**: `تبدیل سریع و آسان واحدهای مختلف (طول، وزن، دما، حجم، زمان، مساحت، سرعت)`
   - **Photo**: (اختیاری) یک آیکون 640x360 پیکسل آپلود کنید
   - **Web App URL**: آدرس Vercel شما (مثلاً: `https://your-app.vercel.app`)
   - **Short name**: `unit-converter` (یا هر نام کوتاه دیگر)

6. BotFather یک لینک به شما می‌دهد که می‌توانید ربات را تست کنید

> 📖 برای راهنمای کامل‌تر، فایل `DEPLOY.md` را مطالعه کنید.

## واحدهای پشتیبانی شده

### طول
متر، کیلومتر، سانتی‌متر، میلی‌متر، مایل، یارد، فوت، اینچ، مایل دریایی

### وزن
کیلوگرم، گرم، میلی‌گرم، تن، تن متریک، پوند، اونس، استون

### دما
سلسیوس، فارنهایت، کلوین

### حجم
لیتر، میلی‌لیتر، متر مکعب، سانتی‌متر مکعب، گالن، کوارت، پینت، فنجان، اونس مایع، قاشق غذاخوری، قاشق چای‌خوری

### زمان
ثانیه، میلی‌ثانیه، دقیقه، ساعت، روز، هفته، ماه، سال، دهه، قرن

### مساحت
متر مربع، کیلومتر مربع، سانتی‌متر مربع، میلی‌متر مربع، مایل مربع، یارد مربع، فوت مربع، اینچ مربع، هکتار، جریب

### سرعت
متر بر ثانیه، کیلومتر بر ساعت، مایل بر ساعت، فوت بر ثانیه، گره، ماخ

## امنیت

- ✅ Sanitization تمام ورودی‌های کاربر
- ✅ جلوگیری از XSS attacks
- ✅ اعتبارسنجی کامل ورودی‌ها
- ✅ مدیریت خطا با پیام‌های فارسی
- ✅ محدود کردن دقت اعشار برای جلوگیری از overflow

## بهینه‌سازی

- ✅ Code splitting برای کاهش bundle size
- ✅ Lazy loading کامپوننت‌ها
- ✅ استفاده از useMemo و useCallback
- ✅ بهینه‌سازی انیمیشن‌ها
- ✅ Minification و Compression
- ✅ Cache headers برای assets

## توسعه

### اضافه کردن واحد جدید

1. فایل تبدیل مربوطه را در `src/utils/conversions/` ویرایش کنید
2. واحد جدید را به `src/constants/units.js` اضافه کنید
3. نام فارسی و نماد واحد را تعریف کنید

### اضافه کردن دسته جدید

1. فایل تبدیل جدید در `src/utils/conversions/` ایجاد کنید
2. دسته را به `src/constants/units.js` اضافه کنید
3. دسته را به `src/utils/conversions/index.js` اضافه کنید

## مجوز

این پروژه برای استفاده عمومی آزاد است.

## تغییرات اخیر

### حذف صفحه Welcome
- **حذف کامل صفحه Welcome**: 
  - صفحه Welcome و تمام کامپوننت‌های مرتبط (Welcome.jsx و WelcomeCircle.jsx) حذف شدند
  - state مربوط به `showWelcome` از App.jsx حذف شد
  - تابع `handleStart` حذف شد
  - import Welcome حذف شد
  - اپلیکیشن مستقیماً با صفحه اصلی شروع می‌شود
  - Back button handler در useTelegram.js ساده‌تر شد
- **بهبود رندسازی اعداد بر اساس نوع واحد**: 
  - رندسازی حالا بر اساس category و unit انجام می‌شود
  - **دما**: 1 رقم اعشار (مثلاً 371.1 درجه سلسیوس)
  - **طول**: 
    - میلی‌متر: بدون اعشار
    - سانتی‌متر: 1 رقم
    - متر، کیلومتر، مایل: 2 رقم
  - **وزن**: 
    - میلی‌گرم: بدون اعشار
    - گرم: 1 رقم
    - کیلوگرم، تن: 2 رقم
  - **حجم**: 
    - میلی‌لیتر: بدون اعشار
    - قاشق، فنجان: 1 رقم
    - لیتر، متر مکعب، گالن: 2 رقم
  - **زمان**: 2 رقم اعشار
  - **مساحت**: 2-3 رقم اعشار (بسته به اندازه)
  - **سرعت**: 1-2 رقم اعشار (بسته به اندازه)
  - صفرهای اضافی در انتهای اعشار حذف می‌شوند
  - اعداد خیلی بزرگ یا خیلی کوچک به صورت نماد علمی نمایش داده می‌شوند
- **حذف هدر صفحه اصلی**: 
  - کامپوننت Header از صفحه اصلی حذف شد
  - فضای بیشتری برای محتوای اصلی در دسترس است
  - padding-top به main اضافه شد تا فاصله مناسب از بالای صفحه حفظ شود
- **تغییر موقعیت کلید سرعت**: 
  - کلید "سرعت" حالا در ردیف سوم ستون وسط قرار دارد (زیر کلید "زمان")
  - از `col-start-2` برای قرار دادن سرعت در ستون وسط استفاده شده است
- **اضافه کردن نسخه در پایین صفحه**: 
  - متن کوچک "نسخه 1.0" در پایین صفحه اصلی اضافه شد
  - استایل: `text-xs text-white/50` برای نمایش کوچک و نیمه‌شفاف
- **رفع مشکل rendering فونت فارسی (سیاهی روی حرف اول)**: 
  - اضافه کردن `text-rendering: optimizeLegibility` برای بهبود rendering فونت فارسی
  - اضافه کردن `-webkit-font-smoothing: antialiased` و `-moz-osx-font-smoothing: grayscale` برای smoothing بهتر
  - اضافه کردن `text-shadow` قوی‌تر برای رفع مشکل سیاهی: `0 0 1px rgba(255, 255, 255, 0.3), 0 0 2px rgba(255, 255, 255, 0.2)`
  - اضافه کردن `-webkit-text-stroke: 0.3px transparent` برای outline نامرئی
  - اضافه کردن `transform: translateZ(0)` برای hardware acceleration
  - اضافه کردن `backfaceVisibility: hidden` و `willChange: transform` برای بهبود rendering
  - اضافه کردن `isolation: isolate` برای جلوگیری از تداخل rendering
  - این تغییرات در ResultDisplay، UnitSelector ها و CSS عمومی اعمال شد
- **اضافه کردن دکمه Share و تغییر Copy به انگلیسی**: 
  - دکمه Copy حالا به انگلیسی "Copy" نمایش داده می‌شود
  - دکمه Share جدید اضافه شد که کنار دکمه Copy قرار دارد
  - دکمه Share متن تبدیل را با فرمت RTL مناسب share می‌کند: `{inputValue} {fromUnit} میشه {result} {toUnit}\n\n\nتبدیل شده با مینی اپ تبدل واحد تلگرام`
  - استفاده از Zero-Width Non-Joiner (\u200C) برای جلوگیری از جابجایی فارسی/انگلیسی
  - لینک از متن share حذف شده و فقط در URL parameter قرار دارد تا Telegram بتواند preview card را بسازد
  - لینک share به `https://t.me/conversion_miniapp_bot/convertor` تنظیم شده است
  - در Telegram از `openTelegramLink` برای share استفاده می‌شود و لینک در preview card نمایش داده می‌شود
  - در مرورگرهای دیگر از Web Share API یا clipboard fallback استفاده می‌شود

## پشتیبانی

برای گزارش باگ یا پیشنهاد ویژگی جدید، لطفاً Issue ایجاد کنید.

---

ساخته شده با ❤️ برای کاربران فارسی‌زبان
